{
  "name": "clawd-jev-trader",
  "version": "0.1.0",
  "description": "Clawd JEV trader on Solana: a TypeSafe Jev model watches the Phoenix SOL-USDC order book and answers buy or sell every slot.",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "probe:book": "bun run scripts/probe-book.ts",
    "test": "for f in scripts/test-*.ts; do bun run \"$f\" || exit 1; done && tsc --noEmit",
    "test:integration": "INTEGRATION=1 bun run scripts/test-decision.ts"
  },
  "dependencies": {
    "@ai-sdk/typesafe-ai": "^3.0.6",
    "@ellipsis-labs/phoenix-sdk": "^2.0.3",
    "@solana/web3.js": "^1.98.4",
    "ai": "^7.0.114"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
